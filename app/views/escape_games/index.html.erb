<% @escape_games.each do |eg| %>
  <p><%= link_to eg.title, eg %></p>
  <p><%= eg.description %></p>
  <p>--------------------</p>
<% end %>

<br><br>

<% if current_user_admin? %>
  <section class="admin">
    <%= link_to "Add a New Escape Game", new_escape_game_path, class: "button"%>
  </section>
<% end %>